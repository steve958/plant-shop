import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#54C143'
        },
        secondary: {
            main: '#54C143'
        }
    }
})


export default theme